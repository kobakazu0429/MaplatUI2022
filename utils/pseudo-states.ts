import { PseudoStatesDefault } from "@ergosign/storybook-addon-pseudo-states-html";

export const maplatPseudoParametersWithInteractiveCase = {
  withPseudo: {
    pseudos: [...PseudoStatesDefault, "focus-visible"],
  },
};
